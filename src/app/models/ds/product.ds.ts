export interface ProductDS 
{
  id: string;
  name: string;
  createdAt: string;
  updatedAt: string;
  category: string;
  categoryId: string;
  quantity: number;
  price: number;
  sku: string;
}
